// #6: C Operators | C Programming for Beginners


#include <stdio.h>

main() {

    /*
      Como padrao, em toda linguage ha um conjunto de operadores que permitem realizar
        operacoes sobre os dados. Em C, esses operadores sao divididos em categorias:
        1. Aritmeticos: +, -, *, /, %
        2. Relacionais: ==, !=, <, >, <=, >=
        3. Logicos: &&, ||, !
        4. Bitwise: &, |, ^, ~, <<, >>
        5. Atribuicao: =, +=, -=, *=, /=,
              %=, &=, |=, ^=, <<=, >>=
        6. Incremento e Decremento: ++, --
        7. Condicional: ?:
        8. Outros: sizeof, comma (,), pointer (*), address (&)
        Esses operadores sao usados para realizar operacoes matematicas, comparar valores, e manipular bits.

    
    
    */





    return 0;
}